import { StorefrontProof } from '@/components/StorefrontProof';

export default function StorefrontProofPage() {
  return (
    <main className="min-h-screen bg-slate-50 py-12">
      <StorefrontProof />
    </main>
  );
}
